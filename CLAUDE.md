# See AGENTS.md

This repo's always-on rules live in `AGENTS.md` — read that file. It
applies to every tool/session working in this repo (Claude Code, Cory's
Codex, or otherwise), not just Claude Code specifically. This file exists
only so Claude Code's auto-load mechanism (which looks for `CLAUDE.md`)
finds its way to the real content.
